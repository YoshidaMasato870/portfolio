<div class="container mt-5">
  <div class="row">
    <div class="col-md-3 col-sm-2"></div>
    <div class="col-md-6 col-sm-8">
      <div class="row">
      <div class="col"></div>
      <div class="col-md-6 col-sm-8">
        <h2>アカウント作成</h2>

        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="field mb-2">
            <span class="text-danger">*</span>
            <%= f.label :account_name %><br />
            <div class="d-grid col-12">
              <%= f.text_field :account_name, autofocus: true, autocomplete: "account_name", placeholder: "アカウント名を入力" %>
            </div>
          </div>

          <div class="field mb-2">
            <%= f.label :account_image %>
            <%= f.file_field :account_image %>
          </div>

          <div class="field mb-2">
            <span class="text-danger">*</span>
            <%= f.label :email %><br />
            <div class="d-grid col-12">
              <%= f.email_field :email, autocomplete: "email", placeholder: "メールアドレスを入力" %>
            </div>
          </div>

          <div class="field mb-2">
            <span class="text-danger">*</span> 
            <%= f.label :password %>
            <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %>文字以上)</em>
            <% end %><br />
            <div class="d-grid col-12">
              <%= f.password_field :password, autocomplete: "new-password", placeholder: "パスワードを入力" %>
            </div>
          </div>

          <div class="field mb-3">
            <span class="text-danger">*</span>
            <%= f.label :password_confirmation %><br />
            <div class="d-grid col-12">
              <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "パスワードを再入力" %>
            </div>
          </div>

          <div class="actions">
            <%= f.submit "アカウントを作成", class: "btn btn-primary me-1" %>
            <%= link_to root_path, class: "btn btn-primary" do %>
              キャンセル
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="col"></div>
    </div>
    <div class="col-md-3 col-sm-2"></div>
  </div>
</div>
