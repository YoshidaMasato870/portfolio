<div class="container">
	<div class="row mt-3">
		<div class="col-2"></div>
		<div class="col-8">
			<h2 class="mb-3">プラン一覧</h2>
			<table class="table table-striped">
				<thead>
					<tr>
						<th class="col-1"></th>
						<th class="col-4">プラン名</th>
						<th class="col-6">プラン詳細</th>
						<th class="col-1"></th>
					</tr>
				</thead>
					<% @plans.each do |plan| %>
					<tr>
						<td><%= image_tag plan.image.url, size: "50x50" %></td>
						<td><%= link_to "#{plan.name}", plan_path(plan.id) %></td>
						<td><%= plan.introduction %></td>
						<td>
							<% if user_signed_in? %>
								<%= render "favorites/btn", plan: plan %>
							<% else %>
								<%= plan.favorites.count %>
								<i class="fa fa-thumbs-o-up"></i>
							<% end %>
						</td>
					</tr>
					<% end %>
				<tbody>
				
			</table>
			<div class="d-flex flex-row-reverse">
				<div><%= link_to "ホームに戻る", root_path, class: "btn btn-primary" %></div>
			</div>
		</div>
		<div class="col-2"></div>
	</div>
</div>
